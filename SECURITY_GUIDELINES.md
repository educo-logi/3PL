# 🔒 33PL 플랫폼 보안 및 데이터 관리 상세 보고 (IT팀 전달용)

본 문서는 33PL 플랫폼의 보안 아키텍처, 사용자 인증 관리 체계 및 결제 보안 구현 방식을 요약한 공식 보고서입니다.

---

## 1. 계정 및 패스워드 관리 체계

### 👑 관리자(Admin) 패스워드 관리
*   **보안 아키텍처 변경**: 기존 환경 변수(`.env`)에 노출되던 방식에서 **Supabase Auth(중앙 집중식 인증)** 방식으로 마이그레이션되었습니다.
*   **저장 방식**: 관리자 계정 정보는 소스 코드나 설정 파일에 하드코딩되지 않으며, Supabase 인증 서버 내부에서 **bcrypt 알고리즘**을 통해 안전하게 해싱되어 저장됩니다.
*   **권한 검증**: 로그인 시 서버 측에서 발행된 **JWT(JSON Web Token)** 내의 `app_metadata.role` 필드를 검증하여 관리자 여부를 판별합니다. 이는 클라이언트 측의 변조가 불가능한 서버 검증 방식입니다.

### 👥 일반 회원(창고/고객사) 계정 및 패스워드 관리
*   **신규 가입자**: Supabase Auth가 기본 적용됩니다. 사용자의 비밀번호는 서버에 평문으로 전송되지 않으며, 업계 표준 수준의 강력한 해시 함수(bcrypt)로 암호화되어 관리됩니다.
*   **레거시 사용자 (마이그레이션 대상)**: 기존 시스템과의 호환성을 위해 `SHA-256` 해싱을 일시적으로 허용하나, 가급적 빠른 시일 내에 신규 인증 체계로의 전환을 권고하고 있습니다.
*   **클라이언트 보안**: 로그인 성공 후 브라우저(LocalStorage)에는 사용자 이름, 이메일 등 비민감 정보만 저장하며, **비밀번호(암호화된 해시 포함) 필드는 저장 전 명시적으로 제거**하여 데이터 유출 위험을 최소화했습니다.

---

## 2. 데이터베이스 보안 및 접근 제어

*   **RLS (Row Level Security)**: 모든 데이터베이스 테이블에 RLS 정책이 적용되어 있습니다. 단순히 '로그인 여부'만 체크하는 것이 아니라, "현재 로그인한 사용자가 이 특정 행(Row)에 접근할 권한이 있는가"를 데이터베이스 레벨에서 매번 검증합니다.
*   **API Key 구분**:
    *   **Anon Key**: 클라이언트에서 사용하며, RLS 정책에 의해 엄격히 제한된 데이터만 조회 가능합니다.
    *   **Service Key**: 절대 클라이언트에 노출하지 않으며, 서버사이드(Edge Functions)에서만 사용하여 특수 관리 작업을 수행합니다.

---

## 3. 토스페이먼츠(Toss Payments) 결제 보안

*   **결제 보안 표준**: PCI-DSS 등 국제 결제 보안 표준을 준수하는 토스페이먼츠 SDK를 사용합니다.
*   **카드 정보 무보관**: 사용자의 카드 번호, CVC 등 민감한 결제 정보는 당사의 서버나 데이터베이스를 거치지 않고 직접 토스페이먼츠 보안 서버로 전송됩니다.
*   **인증 및 검증**:
    *   **클라이언트 키**: 공개 가능한 `Client Key`를 사용하여 결제창을 호출합니다.
    *   **시크릿 키**: 입금 확인, 결제 승인 등의 민감한 동작은 서버사이드에서 안전하게 보관된 `Secret Key`를 통해서만 이루어집니다.
*   **위변조 방지**: 결제 요청 시 고유한 `orderId`를 생성하고, 결제 완료 후 서버에서 결제 금액 및 상태를 재검증하여 위변조 가능성을 차단합니다.

---

## 4. 인프라 및 전송 보안

*   **SSL/TLS**: 모든 데이터 통신은 HTTPS 프로토콜(강력한 SSL 암호화)을 필수로 적용하여 전송 구간에서의 스니핑을 방지합니다.
*   **환경 변수 관리**: 배포 환경(Vercel 등)에서 환경 변수 기능을 사용하여 API 키와 시크릿 정보의 물리적 노출을 방지합니다.

---

> [!NOTE]
> 본 플랫폼의 보안 수준은 최신 웹 표준 및 권장 보안 사례를 준수하고 있으며, 지속적인 모니터링과 업데이트를 통해 보안 위협에 대응하고 있습니다. 관련하여 추가 검토가 필요한 기술 문서가 있다면 IT팀의 요청 시 제공 가능합니다.

---

## 5. [IT팀 자체 점검] 시큐어 코딩 (개발 보안 관리) 코드 리뷰 결과

본 항목은 시스템 취약점을 사전에 방지하기 위해 실제 소스코드를 점검한 결과입니다. (고객사, 3PL 업체, 관리자 권한 및 인증 구조 위주 점검)

### ✅ 점검 통과 항목 (안전)
*   **SQL Injection (데이터베이스 주입 공격) 방어**: 
    *   데이터베이스 접근에 Supabase JS 클라이언트(PostgREST API 기반)를 일관되게 사용하고 있습니다. 파라미터 바인딩 처리가 내장되어 있어 악의적인 SQL 구문 삽입 공격에 안전합니다.
*   **시크릿 키 보호 및 하드코딩 방지**: 
    *   `supabaseClient.js`, 결제 연동 설정 등에 사용되는 주요 식별자 및 키는 모두 `import.meta.env`를 통한 환경 변수로 안전하게 분리되어 소스코드 내 하드코딩된 민감 정보가 없습니다.
*   **계정 암호화 및 인증 구조 전환**: 
    *   `authService.js`에 따르면 기존 클라이언트 단 해싱 방식에서 강력한 서버 단 bcrypt 기반의 Supabase Auth 방식으로의 이행(Migration) 구조가 잘 마련되어 있습니다.

### ⚠️ 조치 필요 항목 (취약점 발견)
*   **[XSS] `dangerouslySetInnerHTML` 사용 최소화 필요 (수정 완료)**: 
    *   **위치**: `src/components/FilterSidebar.jsx` (105 라인 부근)
    *   **내용**: 문자열의 줄바꿈(`\n`) 처리를 위해 React에서 권장하지 않는 `dangerouslySetInnerHTML` 속성을 사용 중입니다. 현재는 하드코딩된 필터 제목으로 쓰여 당장의 위험은 낮으나, 추후 사용자 입력값이 포함될 경우 심각한 크로스 사이트 스크립팅(XSS) 취약점이 발생할 수 있습니다.
    *   **조치 방안**: `.split('\n').map(...)` 형태로 React Node를 반환하는 방식으로 리팩토링이 필요합니다.
*   **[접근 통제] 관리자 페이지 등 인가(Authorization) 가드 취약점 (수정 완료)**:
    *   **위치**: `src/pages/AdminDashboard.jsx`, `src/App.jsx` 등
    *   **조치 방안**: UI 컴포넌트 마운트 시 `supabase.auth.getSession()`을 호출하여 실제 유효한 서버 측 세션(JWT)을 확인하는 로직으로 강화해야 합니다. (수정 완료)

---

## 6. [IT팀 자체 점검] 개인정보보호 및 정보유출 방지 대책 점검 결과

본 항목은 개인정보보호법 기반 안전조치 의무 및 데이터베이스 내 민감 정보 보호 체계를 점검한 결과입니다.

### ⚠️ 조치 필요 항목 (취약점 발견)
*   **[DB 설정] RLS(Row Level Security) 전면 개방으로 인한 대량 정보 유출 위험 (수정 완료)**:
    *   **내용**: Supabase 스키마 확인 결과, `warehouses`, `customers` 테이블의 RLS 정책이 `true`(모두 허용)로 설정되어 있거나 비활성화되어 있습니다. 클라이언트 키(Anon Key)만 있으면 누구나 악의적으로 전체 회원의 이름, 연락처, 주소 등을 무단 수집(크롤링)할 수 있는 치명적인 상태입니다.
    *   **조치 방안**: 즉시 각 테이블에 대해 "자신의 계정 데이터만 읽기/수정 가능"하도록 `auth.uid() = auth_user_id` 기반의 강력한 RLS 정책을 수립하고, 타인의 정보 조회는 철저히 열람권(Viewing Pass) API를 통해서만 제한적으로 이루어지도록 접근 제어를 서버(DB) 단에서 차단해야 합니다. (View 및 RPC 도입으로 해결 완료)
*   **[데이터 저장] 개인정보(PII) DB 내 평문 저장**:
    *   **내용**: 비밀번호는 안전하게 해싱되어 있으나, 사용자의 이름(`contact_person`), 연락처(`phone`), 이메일(`email`) 등의 정보가 평문(Plain-text)으로 저장되어 있습니다.
    *   **조치 방안**: 필요에 따라 Supabase의 `pgcrypto` 확장 모듈 등을 활용해 DB 내 민감 정보를 양방향 암호화하여 저장할 것을 권장합니다.
*   **[접속 기록] 관리자 권한의 개인정보 조회 이력 누락**:
    *   **내용**: 플랫폼 이용자의 열람권 소진 이력(`viewing_history`)은 존재하나, **관리자가 대시보드에서 개인정보를 대량 조회하거나 CSV로 다운로드하는 행위**에 대한 접속 및 조회 감사 로그(Audit Log)가 남지 않습니다.
    *   **조치 방안**: 개인정보보호법(안전성 확보조치 기준)에 따라 관리자의 시스템 접속 및 개인정보 취급 이력을 최소 1년 이상 DB에 보관하는 로직을 추가해야 합니다.
